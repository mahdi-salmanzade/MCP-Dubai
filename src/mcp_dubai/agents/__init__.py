"""
MCP-Dubai agent skills.

Agents are higher-level features that compose multiple data and biz tools
to produce richer outputs (bilingual documents, cross-domain reports).
They live alongside `data/` and `biz/` features in the auto-mount tree.

Tier: 4 (cross-feature behavior)
"""
